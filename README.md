
  # CricMate Management System

  This is a code bundle for CricMate Management System. The original project is available at https://www.figma.com/design/Q93SbPqINTLzk5w8dQKX8k/CricMate-Management-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  